apiVersion: v1
kind: ConfigMap
metadata:
  name: gateway-admin-config
data:
  config.yml: |
    spring:
      cloud:
        kubernetes:
          enabled: true
    eureka:
      client:
        enabled: false
